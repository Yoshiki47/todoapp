import React from 'react'
import ReactDOM from 'react-dom'

import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))
// ReactはSPA(シングルページアプリケーション)
// 1つのhtmlを読み込んで、コンテンツの入れ替えやページ遷移などをすべてJSで処理していく

